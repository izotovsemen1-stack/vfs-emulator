# 💾 VFS Emulator

Проект **VFS Emulator** реализует эмуляцию виртуальной файловой системы (VFS) с поддержкой базовых команд оболочки, управления структурой каталогов, сохранения и загрузки состояния.

---

## 🧱 Этапы реализации

### ✅ Этап 1 — Инициализация проекта
- Создана структура проекта.
- Настроены файлы `.gitignore` и `README.md`.
- Подготовлены основные файлы: `main.py`, `config.py`, `vfs_manager.py`.

### ✅ Этап 2 — Базовые операции VFS
- Реализовано создание, удаление и навигация по директориям.
- Добавлены команды:
  - `mkdir <dir>` — создание папки;
  - `rmdir <dir>` — удаление папки;
  - `cd <dir>` — переход между каталогами;
  - `ls` — просмотр содержимого;
  - `pwd` — отображение текущего пути.

### ✅ Этап 3 — Расширение функционала
- Добавлена команда `vfs-save <filename>` для сохранения состояния VFS в JSON.
- Добавлена команда `vfs-load <filename>` для загрузки состояния.
- Добавлена команда `whoami` и обработка ошибок.

### ✅ Этап 4 — Конфигурация и запуск со скрипта
- Реализована возможность запуска с конфигурационным файлом.
- Поддержан аргумент `--script`, позволяющий выполнять команды из файла.

### ✅ Этап 5 — Финальные улучшения
- Добавлен пример скрипта `startup_stage5.txt`.
- Реализован вывод отладочной информации (`[INFO]`, `[DEBUG]`).
- Подготовлена демонстрация работы проекта.

---

## ⚙️ Запуск проекта

### 🔧 Подготовка окружения

```
python -m venv venv
source venv/bin/activate     # Для macOS / Linux
venv\Scripts\activate        # Для Windows
pip install -r requirements.txt
```

## Демонстрация работы
### Запуск со скриптом

```
python main.py --script scripts/startup_stage5.txt
```

### Запуск с готовой файловой системой

```
python main.py --vfs vfs/sample_vfs.json
```

## Скрипт scripts/startup_stage5.txt
- whoami
- pwd
- mkdir projects
- cd projects
- mkdir test
- cd test
- pwd
- ls
- cd ..
- rmdir test
- ls
- vfs-save saved_vfs.json
- exit

## Запуск
```
python main.py --script scripts/startup_stage5.txt
```

## Результат выполнения
```
Welcome to VFS Emulator!
> whoami
user
> pwd
/
> mkdir projects
> cd projects
Changed directory to /projects
> mkdir test
> cd test
Changed directory to /projects/test
> pwd
/projects/test
> ls
(empty)
> cd ..
Changed directory to /projects
> rmdir test
Removed directory: test
> ls
(empty)
> vfs-save saved_vfs.json
VFS saved to saved_vfs.json
> exit
Exiting...
```