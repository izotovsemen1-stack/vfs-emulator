# 🧠 VFS Emulator

Эмулятор командной оболочки UNIX-подобной ОС с поддержкой виртуальной файловой системы (VFS), реализованный на Python с графическим интерфейсом (Tkinter).

---

## 🚀 Возможности

### Этап 1 — REPL
- Графический интерфейс с консолью.
- Парсер команд с поддержкой кавычек.
- Команды-заглушки: `ls`, `cd`, `exit`.
- Обработка ошибок.

### Этап 2 — Конфигурация
- Поддержка аргументов командной строки:
  - `--vfs <путь>` — путь к JSON-файлу виртуальной файловой системы.
  - `--script <путь>` — стартовый скрипт команд.
- Автоматическое выполнение команд из скрипта при запуске.
- Отладочный вывод параметров запуска.

### Этап 3 — Виртуальная файловая система (VFS)
- Загрузка структуры каталогов из JSON.
- Команды:  
  - `ls` — просмотр содержимого каталога.  
  - `cd` — переход между папками.  
  - `mkdir` — создание каталогов.  
  - `vfs-save` — сохранение состояния VFS в JSON.  
- Работа полностью в памяти, без реальных файлов.

---

## 💻 Запуск проекта

```bash
# 1. Клонировать репозиторий
git clone https://github.com/izotovsemen1-stack/vfs-emulator.git
cd vfs-emulator

# 2. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # macOS / Linux
venv\Scripts\activate     # Windows

# 3. Установить зависимости (если появятся requirements.txt)
pip install -r requirements.txt

# 4. Запустить эмулятор
python main.py --vfs ./vfs/default_vfs.json --script ./scripts/startup.txt
